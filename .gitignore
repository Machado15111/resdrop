# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment secrets
.env
server/.env
# Any per-target env file (.env.vercel, .env.local, …). Committed secrets stay
# in git history forever, so match the whole family — never file by file.
.env.*
!.env.example
!server/.env.example

# Old JSON data (now using Supabase)
server/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
